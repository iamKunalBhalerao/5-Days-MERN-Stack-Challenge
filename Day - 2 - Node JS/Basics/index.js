const fs = require("fs");

/*
// Write File using Node js

const callback = (err) => {
  if (err) throw err;
};

fs.writeFile(
  "new.text",
  "This is file created using node js",
  "utf-8",
  (err) => {
    if (err) throw err;
}
);
*/

/*
// Read File using Node js

fs.readFile("new.text", "utf-8", (err, data) => {
  if (err) throw err;
  else console.log(data);
});
*/

/*
// Remove file using Node js

fs.unlink("new.text", (err) => {
  if (err) throw err;
});
*/

/*
// append Data in File using Node js

fs.appendFile(
  "new.text",
  " Again New Data Added in file from fs from node",
  "utf-8",
  (err) => {
    if (err) throw err;
  }
);
*/
